# 📋 stock.html 실시간 데이터 연동 작업 지시서

> 이 문서를 통째로 Claude에 붙여넣으면 바로 작업을 시작합니다.
> 원래 대화의 맥락 없이도 실행 가능하도록 자족적으로 작성되어 있습니다.

---

## 🎯 목표 (한 줄)

`stock.html`의 DATA_DATE를 자동 갱신하고, RSI·52주 최고가 대비 비율·주가를 GitHub Actions로 매일 받아와 `data.json`에 반영한 뒤 페이지 로드 시 fetch하도록 수정.

## 📁 대상 파일

- **메인**: `/sessions/funny-inspiring-fermi/mnt/주식/stock.html` (1660+ 줄 단일 HTML, 프로젝트 규칙 1번: "stock 파일 기준")
- **신규**: `/sessions/funny-inspiring-fermi/mnt/주식/.github/workflows/update-stock-data.yml`
- **신규**: `/sessions/funny-inspiring-fermi/mnt/주식/scripts/fetch_stock_data.py`
- **신규**: `/sessions/funny-inspiring-fermi/mnt/주식/data.json` (Actions가 덮어쓰는 파일, 초기 커밋용 샘플만 생성)

## ⚠️ 작업 전 필수 준수 사항 (프로젝트 규칙)

1. **규칙 2번**: 작업 시작 전 **구현 계획을 먼저 말하고 사용자 승인**을 받는다. 곧바로 수정 금지.
2. **규칙 3번**: 모르는 부분은 모른다고 명시. 특히:
   - `yfinance` 라이브러리의 현재 Yahoo Finance 차단 정책
   - GitHub Actions `ubuntu-latest` 기본 이미지의 Python 버전
   - `data.json`을 브라우저가 로컬에서 fetch할 때의 CORS 제약 (file:// 프로토콜 이슈)
   이 부분들은 **추측 대신 확인 필요 사항으로 표기**하고 진행한다.

## 🧩 현재 코드 컨텍스트 (바뀐 뒤 건드리지 않아도 되도록 정리)

- 파일 상단에 이미 아래 상수가 있음:
  ```js
  const DATA_DATE = "2026-04-19";  // 이 줄을 수정 대상
  const SIGNAL_RANK = Object.freeze({ buy:0, hold:1, sell:2 });
  const NORM_ETF = { ... };
  const DEFAULT_WEIGHTS_ETF = { ... };
  ```
- `Database` 객체는 이미 아래 메서드를 갖고 있음:
  - `Database.stocks` — 140+ 종목 정적 배열
  - `Database.ETF_EXPENSE`, `ETF_MDD`, `ETF_VOLUME` — ETF 보조 데이터 맵
  - `Database.enrich()` — 각 종목에 `rsi14`, `pct52wHigh`, ETF 지표 defaults 주입
- `App.init()`에서 `Database.enrich()`가 `recomputeAll()` 전에 호출됨
- 섹터 종류: Tech / Communication / Consumer / Healthcare / Financial / Energy / Industrial / Utilities / Materials / RealEstate / ETF

## 📝 작업 항목 (순서대로)

### Step 1. `stock.html` 수정 — DATA_DATE를 `document.lastModified` 기반으로 변경

```js
// 기존
const DATA_DATE = "2026-04-19";

// 변경 후 — 파일 수정일 자동 반영. data.json 로드 성공 시 덮어씀
let DATA_DATE = new Date(document.lastModified).toISOString().slice(0,10);
```

- `const` → `let`으로 바꿔야 하므로 `DATA_DATE` 참조하는 모든 곳 검사 (CSV 파일명 등).
- 헤더의 `<span id="data-date">데이터 기준일: 2026-04-19</span>` 하드코딩 문자열도 JS에서 동적으로 갱신하도록 `App.init`에 아래 추가:
  ```js
  const dateEl = document.getElementById("data-date");
  if (dateEl) dateEl.textContent = `데이터 기준일: ${DATA_DATE}`;
  ```

### Step 2. `stock.html`에 데이터 로더 추가

`Database` 객체에 아래 메서드를 추가한다 (enrich 위에 배치):

```js
async loadRemote(url = "data.json") {
  try {
    const res = await fetch(url, { cache: "no-cache" });
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    const payload = await res.json();
    if (!payload || !Array.isArray(payload.stocks)) throw new Error("invalid schema");

    // 티커 기준으로 병합 (API로 받은 값만 덮어씀, 나머진 정적 데이터 유지)
    const byTicker = new Map(payload.stocks.map(x => [x.ticker, x]));
    this.stocks.forEach(s => {
      const r = byTicker.get(s.ticker);
      if (!r) return;
      if (typeof r.currentPrice === "number") s.currentPrice = r.currentPrice;
      if (typeof r.rsi14        === "number") s.rsi14        = r.rsi14;
      if (typeof r.pct52wHigh   === "number") s.pct52wHigh   = r.pct52wHigh;
      if (typeof r.mdd1y        === "number") s.mdd1y        = r.mdd1y;
      if (typeof r.avgVolume    === "number") s.avgVolume    = r.avgVolume;
    });

    if (payload.generatedAt) {
      DATA_DATE = String(payload.generatedAt).slice(0,10);
    }
    return { ok: true, count: payload.stocks.length };
  } catch (err) {
    console.warn("[Database.loadRemote] 실패 — 정적 데이터로 폴백:", err.message);
    return { ok: false, error: err.message };
  }
}
```

그리고 `App.init`을 `async`로 바꿔 아래 순서로 호출:

```js
async init() {
  Theme.init();
  WEIGHT = Weights.load();
  await Database.loadRemote();   // 실패해도 정적 데이터로 진행
  Database.enrich();             // loadRemote로 받은 값이 우선, 빈 필드만 기본값 주입
  // 헤더 날짜 갱신
  const dateEl = document.getElementById("data-date");
  if (dateEl) dateEl.textContent = `데이터 기준일: ${DATA_DATE}`;
  App.syncWeightUI();
  App.recomputeAll();
  App.bindEvents();
  App.refresh();
},
```

- `DOMContentLoaded` 핸들러는 `App.init`이 async가 되어도 그대로 동작 (Promise 반환 무시됨).

### Step 3. `scripts/fetch_stock_data.py` 작성

```python
"""
일 1회 실행되어 data.json을 갱신한다.
stock.html의 Database.stocks에서 티커 목록을 읽고, yfinance로 최신 지표를 받아와 data.json에 저장.
"""
import json, re, sys
from datetime import datetime, timezone
from pathlib import Path

import yfinance as yf
import pandas as pd

ROOT = Path(__file__).resolve().parent.parent
HTML = ROOT / "stock.html"
OUT  = ROOT / "data.json"

def extract_tickers(html: str):
    # stock.html의 "ticker:\"XXX\"" 패턴을 모두 뽑아냄. 순서 보존.
    seen, out = set(), []
    for m in re.finditer(r'ticker:"([A-Z0-9.\-]+)"', html):
        t = m.group(1)
        if t not in seen:
            seen.add(t); out.append(t)
    return out

def compute_rsi(close: pd.Series, period: int = 14) -> float:
    if len(close) < period + 1:
        return None
    delta = close.diff().dropna()
    gain = delta.clip(lower=0).rolling(period).mean()
    loss = (-delta.clip(upper=0)).rolling(period).mean()
    rs = gain / loss.replace(0, 1e-9)
    rsi = 100 - (100 / (1 + rs))
    val = rsi.iloc[-1]
    return None if pd.isna(val) else round(float(val))

def compute_mdd_1y(close: pd.Series) -> float:
    if close.empty: return None
    roll_max = close.cummax()
    dd = (close / roll_max - 1) * 100
    return round(float(-dd.min()), 1)

def fetch_one(ticker: str):
    # yfinance는 점(.)을 하이픈(-)으로 변환 필요 (예: BRK.B → BRK-B)
    yf_sym = ticker.replace(".", "-")
    hist = yf.Ticker(yf_sym).history(period="1y", auto_adjust=False)
    if hist.empty: return None
    close = hist["Close"].dropna()
    if close.empty: return None
    cur = float(close.iloc[-1])
    high52 = float(close.max())
    pct52  = round(cur / high52, 4) if high52 > 0 else None
    volume_dollar = None
    if "Volume" in hist.columns:
        avg_vol = hist["Volume"].tail(20).mean()
        avg_px  = close.tail(20).mean()
        if pd.notna(avg_vol) and pd.notna(avg_px):
            volume_dollar = round(avg_vol * avg_px / 1_000_000, 1)  # $M
    return {
        "ticker": ticker,
        "currentPrice": round(cur, 2),
        "rsi14": compute_rsi(close),
        "pct52wHigh": pct52,
        "mdd1y": compute_mdd_1y(close),
        "avgVolume": volume_dollar,
    }

def main():
    html = HTML.read_text(encoding="utf-8")
    tickers = extract_tickers(html)
    print(f"[info] {len(tickers)} tickers detected", file=sys.stderr)
    stocks, failed = [], []
    for t in tickers:
        try:
            row = fetch_one(t)
            if row: stocks.append(row)
            else:   failed.append(t)
        except Exception as e:
            failed.append(f"{t}({e.__class__.__name__})")
    payload = {
        "generatedAt": datetime.now(timezone.utc).isoformat(timespec="seconds"),
        "source": "yfinance",
        "stocks": stocks,
        "failed": failed,
    }
    OUT.write_text(json.dumps(payload, ensure_ascii=False, indent=2), encoding="utf-8")
    print(f"[ok] wrote {OUT}  success={len(stocks)}  failed={len(failed)}", file=sys.stderr)

if __name__ == "__main__":
    main()
```

### Step 4. `.github/workflows/update-stock-data.yml` 작성

```yaml
name: Update stock data

on:
  schedule:
    # 매일 UTC 22:00 = KST 07:00 (미국 장마감 후)
    - cron: "0 22 * * 1-5"
  workflow_dispatch: {}   # 수동 실행 가능

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install yfinance pandas

      - name: Fetch data
        run: python scripts/fetch_stock_data.py

      - name: Commit data.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- data.json; then
            echo "no changes"
            exit 0
          fi
          git add data.json
          git commit -m "chore(data): auto-update stock data $(date -u +%Y-%m-%d)"
          git push
```

### Step 5. 초기 `data.json` 커밋 (선택이지만 권장)

- 로컬에서 `python scripts/fetch_stock_data.py` 한 번 실행해서 `data.json` 생성 후 함께 커밋.
- 이렇게 하면 GitHub Pages 등에 배포 직후부터 최신 데이터로 동작.

## ✅ 검증 단계 (필수)

1. **JS 문법 검증**: `node -e "new Function(require('fs').readFileSync('stock.html','utf8').match(/<script>([\s\S]*?)<\/script>/)[1])"` 성공해야 함.
2. **loadRemote 폴백 시나리오**: `data.json`이 없어도 페이지가 정상 동작하는지 확인 (console.warn만 출력되고 enrich()가 기본값 채움).
3. **loadRemote 성공 시나리오**: 최소한의 mock `data.json` (2~3종목)으로 fetch 성공 시 `currentPrice`, `rsi14`, `pct52wHigh`가 덮어써지는지 node 스크립트로 시뮬레이션.
4. **DATA_DATE 반영**: `document.lastModified` → `data.json.generatedAt` 순서로 갱신되는지 확인.
5. **CSV 파일명**: `stocks_${DATA_DATE}.csv`가 정상 생성되는지 (기존 코드 호환성).

## 🧠 구현 시 체크해야 할 알려지지 않은 것 (= 모르는 것)

- **yfinance 안정성**: Yahoo Finance는 비공식 엔드포인트라 2026년 현재도 무료로 잘 동작하는지 실행 전 확인 필요. 실패 시 대안: `alpha_vantage`, `finnhub-python`, `twelvedata`.
- **Actions 비용**: public 레포는 무료, private 레포는 분당 과금. 사용자에게 확인 요청.
- **로컬 `file://` 실행 시 fetch 제약**: 로컬에서 더블클릭으로 HTML을 열면 `fetch('data.json')`이 CORS로 실패한다. 이 경우 정적 데이터로 자동 폴백되므로 앱은 동작하지만, 실시간 데이터는 **웹서버(GitHub Pages, 로컬 `python -m http.server` 등)에서 열어야 적용**됨을 사용자에게 안내.
- **티커 변경·상장폐지**: `extract_tickers`는 현재 파일 기준. 필요 시 `failed` 배열을 로그에 남기는 것까지만 하고, 종목 자동 추가/삭제는 범위 밖으로 둠.

## 📤 완료 후 사용자에게 전달할 것

- 수정된 `stock.html` 경로
- 신규 파일 3개 경로
- GitHub 저장소 연결 방법 (git init → GitHub 리포 생성 → push) **1회성 안내**
- GitHub Pages 배포 옵션 안내 (Settings → Pages → main branch)
- 로컬에서 테스트하는 방법: `cd /path/to/주식 && python -m http.server 8080` 후 `http://localhost:8080/stock.html`

---

## 🏁 시작하기 전에 (Claude가 먼저 할 말)

> "위 작업 지시서의 내용을 기준으로 계획을 재확인하고 진행해도 될까요?
> 특히 아래 두 가지가 확정되어야 Step 4·5를 진행할 수 있습니다:
> 1. GitHub 저장소 준비 여부 (새로 만들지, 기존 리포에 추가할지)
> 2. public / private 레포 선택 — Actions 비용 관련
> 그 전까지는 Step 1~3까지만 로컬에 반영하고, Step 4·5는 파일만 작성 후 멈추는 방식도 가능합니다."
